import  DataModel  from "../data";


let sum = function(x, y){
    return x+y;
}

// var getModelInfo = new Promise(
//     function (resolve, reject) {
//        let  res = Sanitised.find({id: ""}
//         )
//         resolve(res); // fulfilled
//         reject(res); // reject
//     }
// );




export{
    sum,
}